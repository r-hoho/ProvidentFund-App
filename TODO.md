# Project TODO

## August 2026 Policy Switch
- [x] **Transition to the NEW 6-month cooldown / 3rd-withdrawal lockout policy** — DONE.
  - [x] Update `Profile.gs` to change the withdrawal cooldown duration from 12 months to 6 months.
  - [x] Update `Profile.gs` to allow up to a 3rd enrollment (lockout after 3rd withdrawal instead of 2nd withdrawal).
  - [x] Update frontend labels/modals in `JS_Withdraw.html` and `Modals_Withdraw.html` to reflect the 3rd withdrawal maximum limit.
  - [x] Verify withdrawal cooldown calculations and lockout checks using the 8th test case.

## Go-live blockers (must do before launch)
- [ ] **Migrate data from the prior platform into the Google Sheet database** — port existing employee + enrollment records from the current/legacy system into the live Sheets (`Users`, `Enrollments`, `Beneficiaries`, and any historical `Audit_Log` if available). Must reconcile to the column schema each sheet expects (column *order* matters — backend reads via `headers.indexOf(...)`). Key fields to map/validate: `Users` (Allstars_ID, Work_Email, Name_English, Business_Title, Hire_Date, Probation_End); `Enrollments` (First_Enrolled_Date, Current_Enrolled_Date, Current_Plan, Investment_Plan, Withdrawal_Count, Last_Withdrawal_Date, Last_Plan_Change_Date); `Beneficiaries` (append-only ledger — Timestamp, Allstars_ID, Work_Email, Beneficiary_Data JSON). Decide handling for already-enrolled members (preserve original enrolment/member-since dates so tenure, match tier, and plan-change locks compute correctly) and anyone mid-cooldown/locked. Verify a sample of migrated users render correctly in the dashboard before cutover.
- [x] **Move `PF_*` template/folder IDs to Script Properties** — done. `Letter.gs` reads the three IDs via `getLetterConfig_()` (Script Properties); IDs no longer in source. Set in the editor under Project Settings → Script Properties; verified live via `testGenerateBeneficiaryLetter()`.
- [~] **Replace placeholder content** — DONE: bank name (BBL Bank), investment-app link (PVD Connext button → `https://pvdconnext.bblam.co.th/bblamone/login-bblamone`, opens new tab), real investment plan names (stored keys renamed at source `Plan 1-4` → **`TAA1-4`**, no dictionary — shows end-to-end in summary/email/letter; descriptions updated in `Modals.html` Step 2). REMAINING: investment document URL on enrollment Step 2 (deferred — no URL yet; see Polish item).
- [x] **Wire Google Analytics (GA4) adoption metrics** — DONE (built live; hashed `user_id`, PDPA-cleared). `Code/Analytics.gs`: `trackEvent`/`trackAppOpen`/`trackFeatureAction`, SHA-256 hashed pseudonymous `user_id` (+ optional salt), best-effort/never-throws, config in Script Properties. Wired: `app_open` in `Main.gs:doGet()`; `feature_action(success|fail)` in all 5 handlers (enroll/change_plan/withdraw/beneficiary/cancel). **Verified live** — `testTrackEvent()` debug endpoint returned `validationMessages:[]`; after sync, real `app_open` (Realtime active users) + real `feature_action` (4×, with `feature`/`outcome`/`engagement_time_msec`/`ga_session_id` params) confirmed in GA4 Realtime. Note: success/fail tracks app-exception outcomes only — business-rule rejections (locked plan, probation, not-found) are NOT counted as `fail` (they're correct enforcement, not app failures), so the fail rate reflects genuine errors.
- [x] **PDPA consent / legal notice at first enrollment** — RESOLVED (2026-06-11): confirmed all good (covered by HR onboarding paperwork); no in-app acceptance step needed. Also clears the GA4 analytics PDPA decision.
- [x] **Confirm beneficiary requirements with the bank** — RESOLVED (2026-06-11): bank confirmed OK. The first required addition was the **home address** field (built; required, threaded through forms/summary/current view/letter). **Update (2026-06-27):** bank then required a **name title prefix** (นาย/นาง/นางสาว/ด.ช./ด.ญ. + Mr./Ms./Mrs.) — added as a required form dropdown merged into `name` at submit (`mergePrefixes`/`splitPrefix`, model unchanged); also added **พี่น้อง (Sibling)** to relationships and **removed อื่นๆ (Other)** from the picker (kept in `REL_LABELS` for legacy display). Done.
- [ ] **Verify the 7 core business-rule test cases** (see Test Cases section) before real users hit those paths.

## Features
- [x] **In-app feedback (star rating)** — after a successful **main** action (Enroll / Change Plan / Withdraw / Update Beneficiaries — NOT Cancel), a modal pops asking for a 1-5 star rating + optional comment, recorded to the `App_Feedback` sheet (create manually, headers in row 1). Named (Allstars_ID/email resolved server-side), after every success, star required + comment optional + Skip always available, best-effort (never blocks the action). Pops via `pendingFeedbackAction` flag consumed in `populateUI` so it lands after the forced home-reload. Files: `Code/Feedback.gs`, `html/JS_Feedback.html`, `feedbackModal` in `Modals.html`, `SHEET_FEEDBACK` in `Config.gs`, include in `Index.html`.
- [x] **Payroll cut-off notice** — for enrollment, contribution plan change, and withdrawal: compute and display the effective date at the point of submission. Rule: submitted on or before the 15th → effective end of this month; submitted on the 16th or later → effective end of next month. A shared helper function `getEffectiveDate()` should handle the logic and be reused across all three flows.
- [x] **"In Progress" pending transactions box** — dashboard element showing the user's submitted-but-not-yet-effective enrollment / plan change / withdrawal, with a Cancel button available until the next 15th 23:59 BKK cut-off. Cancel reverts with no penalty (no 12-month plan-change lock, no withdrawal-count increment). Phase 1 (read-only display) and Phase 2 (Cancel) both shipped. Edit was dropped from scope. See [Proposal - In Progress Pending Transactions](./Proposal%20-%20In%20Progress%20Pending%20Transactions.md).
- [x] **Email confirmations + signed PDF letters** — COMPLETE (Phases A–G all verified live). bilingual (Thai + English) confirmation email on every audit event (submit, cancel) for all actions. For Enrollment and Beneficiary submissions: capture a drawn signature in-app (signature_pad canvas, dark-blue ink, 500×200, any stroke OK), generate a bilingual PDF letter from a Google Docs template with the signature embedded, attach to the email, archive in Drive. Plan change and Withdrawal: email only, no signature/PDF. Email/letter failure must never block the action. See [Proposal - Email Confirmations and Signed Letters](./Proposal%20-%20Email%20Confirmations%20and%20Signed%20Letters.md).
  - Progress: **Phases A–F shipped (all verified live).** A–C: beneficiary audit upgrade; `Email.gs`; confirmation + cancellation emails for change-plan / withdrawal / cancel; `patchAuditEventData` records `emailSent`. Emails are now **HTML** (styled responsive table via `buildHtmlEmail()`, all values `escapeHtml`'d) — upgraded from the original plain text. **D**: `Letter.gs` Doc→PDF (placeholder fill, plain-text beneficiary list — table dropped, signature embedded scaled to fit `PF_SIG_MAX_WIDTH × PF_SIG_MAX_HEIGHT` preserving aspect, Drive archive); template/folder IDs hard-coded as `PF_*` consts (Script Properties deferred to G). **E**: `processEnrollment` generates the letter + emails the PDF; enrollment wizard gained **step 5 = signature** (`JS_Signature.html` shared pad over signature_pad CDN). **F**: `processUpdateBeneficiaries` generates the letter + emails the PDF; cancel-line gate excludes beneficiary; beneficiary manager gained a **Sign view (B→Next→D Sign→Save)**; beneficiary now uses its own page-2-only template (`PF_BENEFICIARY_TEMPLATE_ID`).
  - **Phase G complete — all verified live.** ✅ Done: `PF_*` IDs moved to Script Properties (`getLetterConfig_()` in `Letter.gs`, verified live); trimmed beneficiary template (`PF_BENEFICIARY_TEMPLATE_ID` set); signature auto-trim (`trimToInk` in `JS_Signature.html`); **letter-failure handling** — on `letterError`, the user still gets the normal confirmation email (intentionally unchanged: still says "letter attached", no attachment — the missing PDF is itself the user's signal), AND a second `sendLetterFailureAlert()` email goes to admin (CC user) with the user/action/txId/raw error. Best-effort, never blocks the action. **Verified live: a forced letter failure produced exactly two emails — the normal confirmation + the admin alert (CC user).** No in-app toast (deliberately lean — earlier amber-toast attempt was removed).
- [x] **Withdrawal modal — dual eligibility display** — modal now renders two labeled rows inside the benefits box: "Your contributions + returns" (always green/eligible) and "Employer match + returns" (gated by 5-year vesting). Display-only change; no business-rule or backend impact.
- [x] **Change Investment Plan info modal** — DONE. `openChangeInvest()` opens `changeInvestModal` (bilingual; bank = **BBL Bank**, app = **PVD Connext**) with a "PVD Connext" link (`<a target="_blank">` → `https://pvdconnext.bblam.co.th/bblamone/login-bblamone`). No backend write/audit/lock — bank app is source of truth post-enrollment.
- [x] **Google Analytics (GA4) — adoption metrics for management** *(DONE — see blocker entry above; `Code/Analytics.gs` shipped, verified live in isolation)* — goal is aggregate "is this app being used successfully": how many visit, who, when, what feature, success rate, fail rate, returning vs new. NOT detailed UI funnel/drop-off. **Approach: server-side GA4 Measurement Protocol** (POST to `/mp/collect` via `UrlFetchApp` from `.gs`), NOT client-side `gtag.js` — the GAS web app renders in a sandboxed iframe (`googleusercontent.com`) where GA's third-party cookies are often blocked, making client-side users/sessions/returning unreliable. Server-side knows the user (`Session.getActiveUser().getEmail()`) and can set a stable GA4 `user_id`, so **returning-vs-new works despite the iframe cookie problem**. Two events cover the whole list: `app_open` in `Main.gs:doGet()` (visits / who / when / returning) and `feature_action` with params `feature` + `outcome` in each handler (enroll/change plan/withdraw/beneficiary → success/fail rate). Build = one helper `trackEvent(name, params)` + ~6 call sites; store Measurement ID + API secret in **Script Properties** (same mechanism as the Phase-G PF_* migration). **PDPA decision needed:** send a **hashed `user_id`** (pseudonymous — recommended) not raw email/ID; keep any hash→name mapping internal. Also confirm routing employee behaviour to Google is acceptable at all — if named per-person usage is wanted, that belongs in the in-house [[System_Log]] (GA's UI is aggregate-only), so GA and the System/operational log are complementary, not either/or.

## Good to have (look into later)
- [ ] **GA4 — segment usage by device + department** — extend the existing `Code/Analytics.gs` tracking with two extra dimensions so management can break `app_open`/`feature_action` down further. Both follow the same "feed a value into the MP payload" pattern; both are best-effort/never-throw like the rest of Analytics. After wiring, register the matching custom dimensions in GA4 (Admin → Data display → Custom definitions) — and remember they collect **going-forward only**, not retroactively.
  - **Track Department** (user-scoped). Requires a data step first: **add a `Department` column to the `Users` sheet** (it doesn't exist today; current cols = Allstars_ID, Work_Email, Name_English, Business_Title, Hire_Date, Probation_End) and populate it with **coarse, consistent** values (fixed list e.g. Finance/Operations/HR/Tech — keep coarse to avoid small-headcount re-identification under PDPA). Then in `Analytics.gs` look the active user up in `Users` by email and send `department` as a GA4 **user property** (`user_properties`, not an event param — it's an attribute of the person). Register as **User-scoped** custom dimension. Column order doesn't matter (backend reads via `headers.indexOf`). Optional: also `business_title` (already populated, but more re-identifying — only if banded). PDPA note: department is group-level/aggregate, generally OK; never send raw name/email/Allstars_ID.
  - **Track Mobile vs Desktop** (event-scoped). **Not auto-captured** — server-side Measurement Protocol hits carry no browser user-agent, and Apps Script `doGet(e)` doesn't expose request headers, so the device must be detected **client-side** and passed through. Plan: in `JS.html` classify on load (`navigator.userAgent` test, or `window.innerWidth < 768`) → `'mobile'|'tablet'|'desktop'`; pass that into the `google.script.run` action calls so `trackFeatureAction` can attach `device_category` as an **event param**; register as **Event-scoped** custom dimension. Wrinkle: `app_open` fires server-side in `doGet()` so it can't get device that way — either add a small client-initiated ping (`google.script.run.trackAppOpenDevice(dev)`) after render, or just track device on `feature_action` only (which already answers "are people enrolling/withdrawing on phone vs desk?").
- [ ] **System / operational log (separate from business audit)** — a forensic log to review later: session starts, action attempts, and success/failure outcomes. Keep it in a **new `System_Log` sheet** — do NOT merge into `Audit_Log`, which is a clean business audit (one row per *successful* transaction, rich domain data). Proposed columns: `Timestamp` (BKK), `Request_ID` (ties an attempt to its outcome), `Email`, `Allstars_ID`, `Category` (SESSION/ACTION/READ/ERROR), `Action` (Enroll/ChangePlan/Withdraw/doGet…), `Outcome` (ATTEMPT/SUCCESS/FAILURE), `Message` (error text/note), `Transaction_ID` (correlate back to `Audit_Log` on success), device/user-agent + viewport. One helper `logSystemEvent(...)`. GAS notes: no real sign-in/logout exists — log a `SESSION_START` in `doGet()` as the closest proxy; every `google.script.run` call is a stateless fresh invocation, so generate a `Request_ID` at the start of each action and reuse it for the matching SUCCESS/FAILURE row.
  - **Device-data question (deferred):** device/`Metadata` is forensic, so it belongs in `System_Log` going forward. But do NOT gut the existing `Metadata` column from the business audit as step one — order is: stand up `System_Log` → verify it captures device data on every action with a matching `Transaction_ID` → *then* decide per-action. Lean toward **keeping** device context on the business record (or guaranteeing the System_Log row is never pruned) for **signature-bearing actions (Enroll, Beneficiary)** due to non-repudiation / legal weight; low-stakes actions (ChangePlan, Withdraw) can rely on the `Transaction_ID` join into `System_Log`.
- [ ] **Help / FAQ section** — bilingual static content explaining the plan basics: employer match tiers (3/5/7/10% by tenure), 5-year vesting rule for employer match on withdrawal, the 15th cut-off, the bank-app split for investment plan changes, the 6-month plan-change lock, withdrawal cooldown / lifetime limit. Accessible from the dashboard (e.g. a "?" or "ช่วยเหลือ / Help" link).
- [ ] **Help / issue report FAB** — round floating action button with `?` symbol, `position: fixed` bottom-right, always visible **except** when a wizard/modal is open (hide via a `.wizard-open` class on the wizard root). Click opens a bilingual modal with a single textarea (~1000 char limit), Submit + Cancel. Submit calls the existing backend `reportIssueToAdmin()` in `Utils.gs` and shows a success toast. Email auto-attaches as much context as possible: `Allstars_ID`, `Name_English`, `Work_Email`, current state pill (Locked / Probation / Cooldown / Enrolled / Not Enrolled), `Current_Plan` %, initial `Investment_Plan`, `Withdrawal_Count`, `Hire_Date`, `Probation_End`, Member Since date, `Last_Withdrawal_Date`, `Last_Plan_Change_Date`, browser user agent, viewport size, timestamp (Asia/Bangkok). Strip/escape HTML from user input before embedding. Reuse the same modal from the existing error-state "แจ้งปัญหา" button so there is one unified report flow. Supersedes the polish item for the `sendReport()` stub.
- [ ] **State-transition notification emails** — e.g. "your probation ends today, you can now enroll" or "your withdrawal cooldown ends today". Hooks into the email helper from [Proposal - Email Confirmations](./Proposal%20-%20Email%20Confirmations.md) once that ships. Needs a scheduled trigger (daily) to scan for users crossing a threshold.
- [x] **PDPA consent / legal terms** display during first enrollment — RESOLVED (2026-06-11): covered by existing HR onboarding paperwork; no in-app acceptance step required.
- [ ] **User-downloadable enrollment summary** (PDF or printable view) — for users who want their own record beyond the email confirmation. Includes current plan %, investment plan (initial selection), beneficiaries, member-since date, employer match tier.
- [ ] **HR / admin read-only view** — in-app dashboard for HR (gated by email allowlist) to view all employees' status without querying the sheet directly. Could be paired with implementing the unused `Monthly_Reporting` sheet.

## Test Cases
- [ ] Enrollment: submit with beneficiary % that doesn't add up to 100% — wizard Next button should stay disabled
- [ ] Enrollment: user still on probation — Enroll button should not appear
- [ ] Plan change: attempt to change within 6 months — locked modal should appear with correct eligible date
- [ ] Plan change: select the same % as current — Confirm button should be disabled
- [ ] Withdrawal: user with < 5 years tenure — modal should show "not eligible for employer match" in red
- [ ] Withdrawal: complete a 3rd withdrawal — status should permanently show "หมดสิทธิ์ถาวร / Locked" with no action buttons
- [ ] Withdrawal: complete a 2nd withdrawal — status should show 6-month "ระงับสิทธิ์ชั่วคราว / Cooldown" (NOT permanent lockout), then allow a 3rd enrollment
- [ ] Member since: user who has withdrawn once and re-enrolled — "Member Since" date and duration should reflect 2nd enrollment date, not hire date

## Polish
- [~] **Rethink cut-off / effective-date messaging** — the month-end date (e.g. "30 Jun 2026") misread as a literal money-movement day. **Banner + emails DONE; letters keep precise date.** Reframed to a payroll **month**, with action-specific copy:
  - Enroll / plan change → "First applies to payroll for / เริ่มหัก/มีผลกับเงินเดือนรอบ: มิถุนายน 2026 / June 2026" (FIRST applicable payroll).
  - Withdrawal → "Final contribution deducted / หักเงินสมทบครั้งสุดท้าย: …" (LAST contribution month — submit ≤15th → this month is the last deduction; ≥16th → deducted this month *and* next, so next month is the last). Same cut-off math, flipped meaning.
  - Frontend: `html/JS_Utils.html:getEffectiveDateInfo` returns `{withinCutoff, monthTH, monthEN}`; `effectiveDateBannerHTML(context)` takes a `context` arg (`'withdraw'` flips the copy); `JS_Withdraw.html` passes `'withdraw'`, enrollment + change-plan use the default.
  - Backend: new `Code/Utils.gs:getEffectiveMonthLabel(submittedAt)` → `{th, en}` bilingual month (mirrors `getEffectiveDate`'s cut-off rule). Emails (`Email.gs`, all 3 SUBMITTED blocks) now use `details.effectiveMonth`; callers (`Action.gs` enroll+change, `Withdraw.gs`) pass `getEffectiveMonthLabel(today)`.
  - **Enrollment PDF letter** also switched to month framing: enrollment `ctx` passes `effectiveMonth: getEffectiveMonthLabel(today)`; `Letter.gs:buildPlaceholderMap` fills `{{effective_date}}` with the bilingual month ("มิถุนายน 2026 / June 2026") when `ctx.effectiveMonth` is present, else falls back to `ctx.effectiveDate`. Test harness `testGenerateLetter()` updated to the month object.
  - **Deliberately unchanged:** the **beneficiary** letter (no payroll cut-off → still passes `effectiveDate: fmtDate(today)`, an immediate precise date) and the **audit log** keep the precise "dd MMM yyyy" value. `getEffectiveDate()` is still used for those.
  - **Template edit still needed (Drive, not code):** the static label *next to* `{{effective_date}}` in the **enrollment Google Doc template** still reads "วันที่มีผล / Effective date" — update it to match the new wording ("เริ่มหัก/มีผลกับเงินเดือนรอบ / First applies to payroll for") so the label and the now-month value agree.
  - Needs live verification after manual sync (banner on all 3 flows + the 3 confirmation emails + the enrollment PDF via `testGenerateLetter()`).
- [ ] **Inline info toggles for key figures (e.g. employer match)** — let users tap a small "ℹ️" next to a value to reveal an explanation, then dismiss when done. Example: employer match `3%` → expand to "based on your tenure; see tier table (3/5/7/10%)…". Preferred lightweight approach: **native `<details>`/`<summary>`** (no JS, expand/collapse in place, handles a small table) — or **Pico `data-tooltip`** for short one-liners only (no tables/buttons). Bilingual. (Heavier alternatives considered but deferred: `<dialog>` modal with an explicit "Got it" button, or routing to the future Help/FAQ section.) Candidate spots: employer match %, 5-year vesting, 15th cut-off, plan-change lock.
- [x] **In Progress box — drop "(BKK)" from cancellable date** — done; removed the trailing ` (BKK)` from the "Cancellable until: …" line (`JS.html:72`).
- [x] **Display beneficiary relationship in Thai** — done. Shared `relLabel()` + `REL_LABELS` map (Thai-first, English in brackets) added in both `Code/Config.gs`+`Code/Utils.gs` (backend) and `html/JS_Utils.html` (frontend, keep the two maps in sync). Wired into summary (`JS.html`), current view (`JS_Beneficiary.html:renderCurrentBeneficiaries`), `Email.gs` beneficiary lines, and `Letter.gs:fillBeneficiaryList`. Stored `rel` key stays English (still the `<select>` option value); only display is mapped; unknown/blank keys pass through. History view shows no `rel` so it was untouched.
- [x] **Confirm beneficiary requirements with the Bank** — DONE (bank confirmed 2026-06-11). Required **address** field built: `{name, rel, pct, address}` threaded through both forms (textarea + "Same as Above" copy checkbox, `renderBeneficiaries`/`renderUpdateBeneficiaries`), validation (`validateStep`/`validateUpdateBen` — required, and each pct must be ≥1%), Step 4 summary, current view, and the PDF letter. Email intentionally omits address. Storage/audit forward-compatible (inputs use `address || ''`). **Update (2026-06-27):** added a required **title prefix** (นาย/นาง/นางสาว/ด.ช./ด.ญ. + Mr./Ms./Mrs.) merged into `name` at submit (`mergePrefixes`/`splitPrefix` in `JS.html`, shared by wizard + manager; model unchanged), added **พี่น้อง (Sibling)** relationship, and **removed อื่นๆ (Other)** from the picker (retained in `REL_LABELS` for legacy display). Files: `JS.html`, `JS_Beneficiary.html`, `JS_Utils.html`, `Config.gs`.
- [x] **Rename investment plans 1/2/3/4 to real names** — DONE. Renamed the stored keys at source `Plan 1-4` → **`TAA1-4`** (direct replace in `Modals.html` Step 2 cards: `selectInvestment('TAA1')` + `id="card-i-TAA1"`), with Thai descriptions. No dictionary needed — every downstream render (summary, email, letter) echoes the stored value, so they all show `TAA1` automatically. Safe because pre-launch with no production data; migration maps legacy → `TAA1-4` as canonical.
- [ ] **Investment document link on enrollment Step 2 of 5** — add a prominent link to the investment plan document so every user reads it before submitting. Style: blue highlighted text with an "open in new tab" (↗ / external-link) icon, `target="_blank" rel="noopener"`. Goal is awareness — make it visually obvious and encourage the click before proceeding. Bilingual label. Document URL is a placeholder until provided.
- [~] **"Available on / date left" for ineligible users** — DATE DISPLAY DONE for **probation** + **first-withdrawal cooldown**: `populateUI` shows "Eligible to enroll after / re-enroll on: `<date>`" (`probationEndDate` / `cooldownEndDate` from `getUserProfile`) in `#cooldownMessage`. **Bug fixed (2026-06-11):** the message div was nested inside `#enrolledActionsGroup` (`display:none` for non-enrolled users) so it could never show — moved it directly into the Actions `<article>`; Thai/Eng/date split onto separate `<br>` lines for mobile. REMAINING (optional): the "X days/months left" **countdown**; and the **plan-change lock** date isn't surfaced this way (it has its own locked modal via `checkPlanChangeEligibility`).
- [x] Stale variable declarations in `populateUI()` — done; removed the unused `changePlanBtn`/`withdrawBtn` declarations (referenced non-existent `mainChangePlanBtn`/`mainWithdrawBtn`).
- [ ] `Monthly_Reporting` sheet is defined in `Config.gs` but never read or written anywhere — either implement reporting logic or remove the constant
- [ ] Effective date banner — polish styling and spacing across all three flows (enrollment, plan change, withdrawal)
